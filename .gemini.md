# Day To Us - AI Assistant Context

> Quick reference for AI assistants working on this project.

## Project Overview

**Day To Us** - 커플을 위한 생활 공유 PWA 서비스

### 도메인 지식

**핵심 컨셉**: 커플이 일상을 함께 관리하고 공유하는 플랫폼

**핵심 기능 (Core)**:
- **공동 가계부**: 수입/지출 기록, 실시간 동기화, 카테고리별 분석, 월별 예산
- **일정 관리**: 커플 일정 공유, 캘린더 뷰, 카테고리별 일정
- **고정 지출**: 정기 지출 항목 관리 및 자동 반영
- **공간 관리**: 커플 전용 프라이빗 공간 생성 및 참여

**향후 확장**: 사진 앨범, 기념일 관리, 위시리스트

### Tech Stack
- **React** ^19.2.0 + **TypeScript** ~5.9.3
- **Vite** ^7.2.4 (build tool)
- **Tailwind CSS** ^4.1.18 (Styling)
- **React Router** ^7.12.0 (Routing)
- **vite-plugin-pwa** ^1.2.0 (PWA support)
- **ESLint** ^9.39.1
- **axios** ^1.13.2 (HTTP client)
- **date-fns** ^4.1.0 (Date utility)

## Project Structure (Feature-Based)

day-to-us-frontend/
├── public/
├── src/
│   ├── features/               # Feature modules
│   │   ├── auth/              # 인증 (로그인/가입)
│   │   ├── budget/            # 가계부 기능
│   │   ├── fixedExpense/      # 고정 지출 관리
│   │   ├── schedule/          # 일정 관리 기능
│   │   └── space/             # 공간/멤버 관리
│   ├── pages/                 # 페이지 컴포넌트
│   │   ├── BudgetPage.tsx
│   │   ├── CalendarPage.tsx   # 달력 + 가계부/일정 통합 뷰
│   │   ├── DashboardPage.tsx
│   │   ├── FixedExpensePage.tsx
│   │   └── SettingsPage.tsx
│   ├── shared/                # 공통 모듈
│   │   ├── api/
│   │   │   └── client.ts      # Axios instance
│   │   ├── components/        # 공통 UI 컴포넌트
│   │   │   ├── Layout.tsx     # 메인 레이아웃
│   │   │   ├── Modal.tsx      # 모달 컴포넌트
│   │   │   ├── Drawer.tsx     # 모바일 하단 드로어
│   │   │   ├── Toast.tsx      # 토스트 알림
│   │   │   └── ConfirmModal.tsx # 확인 다이얼로그
│   │   ├── hooks/             # 공통 Hooks
│   │   │   └── useOutsideClick.ts # 외부 클릭 감지
│   │   ├── styles/
│   │   └── utils/
│   │       └── format.ts      # 포맷 유틸
│   ├── App.tsx
│   ├── main.tsx
│   ├── index.css              # Global styles & Tailwind
│   └── vite-env.d.ts
├── .env                       # 환경 변수
├── vite.config.ts
└── package.json

**아키텍처 원칙**:
- Feature-based 구조 (기능별 모듈화)
- Shared 폴더로 공통 코드 관리
- 각 feature는 독립적으로 동작 가능
- API 레이어 분리 (client.ts + featureApi.ts)

## Backend API

**Base URL**: http://localhost:8080

### Endpoints (Examples)
- `GET /shared-spaces` - 공간 목록
- `GET /shared-spaces/{spaceId}/budget-entries` - 가계부 목록
- `GET /shared-spaces/{spaceId}/schedules` - 일정 목록
- `GET /shared-spaces/{spaceId}/fixed-expenses` - 고정 지출 목록

## Design System (Tailwind CSS)

### Colors (Blue-Purple Gradient)
Main gradient used across the app:
```css
/* Custom theme variables */
--color-primary-start: #667eea;
--color-primary-end: #764ba2;

/* Utility classes */
.gradient-bg { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
.gradient-text { /* gradient text with clip */ }
```

### Typography
- Font: Inter (Google Fonts)
- Weights: 400, 500, 600, 700

### Animations
- `fade-in` - 0.2s ease-in-out opacity transition
- `slide-up` - 0.2s ease-in-out upward slide with fade

### Spacing Scale
- Tailwind formatted spacing (p-4, m-4, etc.)

## Responsive Design Patterns

### Mobile-First Approach
- **Drawer Component**: Bottom sheet for mobile interactions (date selection, forms)
- **Modal Component**: Desktop dialogs
- **Breakpoint**: `md:` (768px) used to switch between mobile/desktop layouts

### CalendarPage Features
- **Mobile**: 
  - Compact calendar cells with event dots
  - Drawer opens on date click
  - Thinner event bars without text
- **Desktop**:
  - Larger calendar cells with event details
  - Modal dialogs for forms
  - Full event bars with text labels
  - Hover states and + buttons for quick actions

## TypeScript Best Practices

### File Extensions
- **`.tsx`**: React components
- **`.ts`**: Utils, types, non-React code
- **`.d.ts`**: Type declarations

### Type Definitions
```typescript
// Always type component props
interface Props {
  title: string
  onClick: () => void
}

// Use type imports
import type { FC } from 'react'
```

### Path Aliases
- `@/` → `./src/`
- Example: `import apiClient from '@/shared/api/client'`

## React Best Practices

### Component Structure
```typescript
import { useState } from 'react'
import type { FC } from 'react'

interface Props {
  title: string
}

export const Component: FC<Props> = ({ title }) => {
  const [state, setState] = useState('')
  
  return <div className="p-4">{title}</div>
}
```

### Naming Conventions
- **Components**: PascalCase (`BudgetCard.tsx`)
- **Hooks**: camelCase with `use` prefix (`useBudget.ts`)
- **Utils**: camelCase (`formatCurrency.ts`)
- **Types**: PascalCase (`budget.types.ts`)

## PWA Configuration

### Service Worker
- **Strategy**: generateSW (auto-generated)
- **Update**: autoUpdate
- **Dev mode**: Enabled

## Common Tasks

### Add New Feature
```bash
src/features/
└── new-feature/
    ├── api/
    ├── components/
    ├── hooks/
    └── types/
```

### API Call Pattern
```typescript
import { budgetApi } from '@/features/budget/api/budgetApi'

const entries = await budgetApi.getAll(spaceId)
```

## Environment Variables

```env
VITE_API_BASE_URL=http://localhost:8080
```

Access: `import.meta.env.VITE_API_BASE_URL`

---

*Last updated: 2026-02-10*
*Based on React, Vite, Tailwind CSS, and vite-plugin-pwa best practices*
